# TODO: dynamically update inventory using awscli.
all:
  children:
    workstations:
      children:
        kalis:
          hosts:
            # Change range to match current number of kalis
            127.0.0.1
